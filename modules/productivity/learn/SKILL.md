---
name: learn
description: "经验复盘：审查、评分、进化 doc/lessons-learned.md 中的经验条目。建议提升为项目规则，识别重复条目，生成经验报告。触发：用户说'经验复盘'、'/learn'、'review lessons'。"
---

# Learn — 经验复盘与进化

定期审查项目经验教训，评分排序，将高置信度经验提升为项目规则。

## 触发时机

- 用户说"经验复盘"/"review lessons"/"/learn"
- 项目里程碑完成后（用户主动触发）
- 编排流程全部完成后（建议用户运行）

## 工作流程

### Step 1：读取解析

Read `doc/lessons-learned.md`，解析所有条目。

两种格式都需识别：

**结构化格式**：
```
### EXP-{NNN}: {标题}
| 字段 | 值 |
...
```
提取：EXP-ID、标题、域、置信度、命中次数、日期、类型、触发条件。

**旧格式**：
```
- [mmdd] {描述}
  - 原因：...
  - 解法：...
```
标记为"旧格式"，在后续步骤中建议升级。

**命中次数统计**：
从 `doc/main-log.md` 中 Grep 日志行统计每个 EXP-ID 被注入的次数：
```
Grep(pattern="注入经验：.*EXP-{NNN}", path="doc/main-log.md")
```
以此结果更新结构化条目中的"命中次数"字段。

同时检查项目规则：
- Glob(pattern=".claude/rules/*.md") — 检查已有项目规则
- 读取每个规则文件，记录已提升的 EXP-ID

### Step 2：评分排序

对每条经验计算综合评分：

| 维度 | 权重 | 计算方式 |
|------|------|----------|
| 命中率 | 0.4 | 命中次数 / 文件中条目总数（归一化） |
| 置信度 | 0.3 | 直接取置信度值 |
| 类型权重 | 0.2 | anti-pattern: 1.0, best-practice: 0.9, bug-fix: 0.7, prompt-feedback: 0.5 |
| 新鲜度 | 0.1 | 越新越高 |

综合评分 = 各维度加权求和。

### Step 3：识别动作

对每条经验，判断建议动作：

| 动作 | 条件 | 说明 |
|------|------|------|
| PROMOTE | 置信度 ≥ 0.7 且命中 ≥ 3 且尚未提升 | 提升为项目规则 |
| MERGE | 与另一条经验域相同 + 触发条件相似 | 合并为一条 |
| UPGRADE | 旧格式条目 | 升级为结构化格式 |
| KEEP | 不满足其他条件 | 保持现状 |

### Step 4：输出报告

向用户展示复盘报告：

```markdown
## 经验复盘报告

### 总览
- 总条目数：{N}
- 结构化：{X} 条 | 旧格式：{Y} 条
- 平均置信度：{avg}
- 已提升为规则：{Z} 条

### 条目评分
| EXP-ID | 标题 | 置信度 | 命中 | 评分 | 建议动作 |
|--------|------|--------|------|------|----------|

### 建议提升为项目规则
| EXP-ID | 标题 | 域 | 规则摘要 |
|--------|------|-----|---------|

### 建议合并
| 条目组 | 原因 |
|--------|------|

### 建议升级格式
| 旧条目 | 升级后的标题 |
|--------|-------------|
```

### Step 5：执行用户确认的动作

**提升为项目规则**：

用户确认后，写入 `.claude/rules/{domain}-rules.md`：

```markdown
# {Domain} 规则（自动提升自经验）

> 由 /learn 于 {date} 生成。来源条目见每条规则。

## R-{NNN}: {规则标题}

**触发条件**：{什么情况下应用此规则}

**规则内容**：{从经验的解法中提炼的可执行规则}

**来源经验**：EXP-{XXX}

**验证记录**：命中 {N} 次
```

同时更新 `doc/lessons-learned.md` 中对应条目的置信度为 0.9（已提升）。

**合并条目**：

用户确认合并文本后，用 Edit 删除旧条目、更新保留条目的内容。

**升级格式**：

用户确认后，将旧格式条目改写为结构化格式。写入前先 Grep `### EXP-` 获取最大编号。

### Step 6：进化管道状态

报告结束后，展示三级进化管道状态：

```
进化管道：
  Level 1 经验条目：{N} 条（活跃）
  Level 2 项目规则：{X} 条（.claude/rules/）
  Level 3 全局规则：需手动提升到 ~/.claude/rules/

  提升建议：{如有满足条件的条目，列出}
```

## 进化管道（三级）

### Level 1：经验条目（doc/lessons-learned.md）

开发过程中自动积累，Agent 修正后追加。日常存储层。

### Level 2：项目规则（.claude/rules/）

从高置信度经验自动提升，所有 Agent 在工作时都会读取遵守。项目级约束。

提升条件：
- 置信度 ≥ 0.7
- 命中次数 ≥ 3
- 用户确认提升

### Level 3：全局规则（~/.claude/rules/）

跨项目验证的模式，适用于所有项目。手动提升。

建议条件：
- 已在 2+ 个项目中作为项目规则存在
- 用户确认适用于全局

## 注意事项

- ⚠️ 对 `doc/lessons-learned.md` 的所有修改必须使用 Edit 工具，禁止 Write 覆盖
- ⚠️ 提升为规则后，原条目保留在 lessons-learned.md 中（置信度更新为 0.9），不删除
- ⚠️ 旧格式条目不强制升级，仅在用户确认后执行
